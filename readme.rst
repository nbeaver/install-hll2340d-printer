Installation
------------

Run ``make``.

Troubleshooting
---------------

After installing,
try printing a test page.
If it doesn't work,
change the ``lpd:`` URL to match the IP address of the printer.
